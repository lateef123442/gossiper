node_modules
.env
coverage
coverage.json
typechain
typechain-types

# Hardhat files
cache
artifacts

# Deployment files (keep deployments directory structure but ignore sensitive data)
deployments/*.json
!deployments/.gitkeep
