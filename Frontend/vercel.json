{
  "installCommand": "rm -f package-lock.json && npm install --prefer-offline --no-audit && npm install --no-save lightningcss-linux-x64-gnu@1.30.2",
  "buildCommand": "npm run build",
  "framework": "nextjs"
}